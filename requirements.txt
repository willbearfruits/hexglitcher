# HexGlitcher v3 runtime dependencies (canonical list is pyproject.toml).
# Install the app with:  pip install -e .
PySide6>=6.6
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8
# optional, recommended for video — JITs the pixel-sort kernel:
# numba>=0.58
